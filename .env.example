APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:P6KuVm789qBbXD71rT3csMb8W25iT52uXW4irNPLm2k=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US 

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=eventsmap
DB_USERNAME=eventsmap
DB_PASSWORD=secret

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

# IMPORTANT: If using 'database', you MUST run 'php artisan queue:work --queue=emails,default'
# for queued jobs (invitation emails use the 'emails' queue).
# Use 'sync' for immediate processing without a queue worker.
QUEUE_CONNECTION=sync

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=arunkamaraj.zerosoft@gmail.com
MAIL_PASSWORD=mbhtbxsmujibopyr
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=arunkamaraj.zerosoft@gmail.com
MAIL_FROM_NAME="Events Map"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

FRONTEND_URL=http://localhost:5173

STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
# auto (default): subscription if Price is recurring, payment if one-time
STRIPE_CHECKOUT_MODE=auto
STRIPE_PREMIUM_AMOUNT=100
STRIPE_PREMIUM_CURRENCY=eur

# Invoice PDF (company block on receipts)
INVOICE_COMPANY_NAME="${APP_NAME}"
INVOICE_COMPANY_ADDRESS=
INVOICE_COMPANY_VAT_NUMBER=
INVOICE_COMPANY_EMAIL="${MAIL_FROM_ADDRESS}"
# Relative to public/ (recommended) or full URL; falls back to MAIL_LOGO_URL
INVOICE_LOGO_PATH=images/marker.png
INVOICE_NUMBER_PREFIX=INV
INVOICE_DEFAULT_TAX_RATE=0
INVOICE_STORAGE_DISK=public
INVOICE_EMAIL_QUEUE=emails

# Firebase Configuration (for chat authentication)
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# Path is relative to project root, or use absolute path
FIREBASE_CREDENTIALS="F:/Maharaja/Tasks/Event Map/eventsmap-backend/storage/app/eventsmap-d4fe8-firebase-adminsdk-fbsvc-aa723928e8.json"
FIREBASE_PROJECT_ID=eventsmap-d4fe8

VITE_APP_NAME="${APP_NAME}"
