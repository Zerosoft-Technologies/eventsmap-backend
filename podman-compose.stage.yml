version: "3.9"

services:
  backend:
    image: ghcr.io/zerosoft-technologies/eventsmap-backend:${TAG}
    container_name: eventsmap-backend-stage
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4002:8000"
